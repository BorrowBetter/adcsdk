mutation CreateLiveTransfer($input: CreateLiveTransferInput!) {
	createLiveTransfer(input: $input) {
		data {
			sigmaLeadId
			dialerStatus
			dialerProvider
			marketingLeadId
		}
		errors {
			message
		}
	}
}
