mutation CreateMinimalLiveTransfer($input: CreateMinimalLiveTransferInput!) {
	createMinimalLiveTransfer(input: $input) {
		data {
			sigmaLeadId
			dialerStatus
			dialerProvider
			marketingLeadId
		}
		errors {
			message
		}
	}
}
